using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AsteroidMovement : MonoBehaviour
{
    /*
    -1 Dodać animacje rotacji asteroidy bez rotacji obiektem, 
        aby dokleić punkty w których asteroidy dzielą się na mniejsze.
    -2 Jeżeli asteroida mniejsza niż x to znika po kontakcie z pociskiem, 
        a jeżeli dotyka granicy świata to znika niezależnie od rozmiaru.
    */
    
    
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
